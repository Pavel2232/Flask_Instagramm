“ребуютс€ советы дл€
оформите README

`get_posts_by_user(user_name)` Ц возвращает посты определенного пользовател€. ‘ункци€ должна вызывать ошибку `ValueError` если такого пользовател€ нет и пустой список, если у пользовател€ нет постов.

`get_comments_by_post_id(post_id)` Ц возвращает комментарии определенного поста. ‘ункци€ должна вызывать ошибку `ValueError` если такого поста нет и пустой список, если у поста нет комментов.